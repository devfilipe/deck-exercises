# hello-cli

Layer 3: the app. `hello --lang pt` prints the greeting the contract declares.

It holds no greeting and no language name. If you find yourself adding one here,
the change belongs in `hello-commands` and the layering has been broken.
