# storefront-web

The pages a customer sees. It reads the API and renders what comes back; it
knows no field the contract has not published.

TODO: the product page still hard-codes the currency.
TODO: no empty state for a product with no price.
