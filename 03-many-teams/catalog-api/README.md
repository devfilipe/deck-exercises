# catalog-api

Serves what `catalog-schema` declares. It holds no field names of its own:
`routes.json` says which routes exist, and each one names the fields it returns.

TODO: the price route has no pagination.
